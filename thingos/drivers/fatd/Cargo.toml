[package]
name = "fatd"
version = "0.1.0"
edition = "2021"

[dependencies]
stem = { path = "../../stem", features = ["rt", "global-alloc"] }
abi = { path = "../../abi" }
fat = { path = "../fat" }
ipc_helpers = { path = "../../libs/ipc_helpers" }
