[package]
name = "leaf"
version = "0.1.0"
edition = "2021"

[dependencies]
stem = { path = "../../stem", features = ["rt"] }
abi = { path = "../../abi" }
terminal_core = { path = "../../libs/terminal_core" }

[[bin]]
name = "leaf"
path = "src/main.rs"
test = false
bench = false
