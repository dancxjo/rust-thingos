[package]
name = "leaf"
version = "0.1.0"
edition = "2021"

[dependencies]
terminal_core = { path = "../../libs/terminal_core" }

[target.'cfg(target_os = "thingos")'.dependencies]
stem = { path = "../../stem", features = ["rt"] }
abi = { path = "../../abi" }

[[bin]]
name = "leaf"
path = "src/main.rs"
test = false
bench = false
